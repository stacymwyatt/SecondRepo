# Project: SecondRepo — Contact List App

## What this project is
A simple contact list web app. Users can add, view, edit, and delete contacts.
Built for local development and learning purposes.

## Stack
- **Frontend**: HTML, CSS, vanilla JavaScript (index.html)
- **Backend**: Node.js with Express (server.js)
- **Database**: SQLite via better-sqlite3 (contacts.db) — viewable in DBeaver
- **API**: REST API served by Express

## How to run
npm install
node server.js
# Then open http://localhost:3000 in a browser

## Project structure (planned)
- index.html       — the UI
- server.js        — the Express server and API routes
- contacts.db      — the SQLite database file (auto-created on first run)
- package.json     — Node.js dependencies

## Preferences
- Keep code simple and easy to read — user is new to coding
- Add comments to explain what code does
- Prefer small, focused changes
- Test everything locally before pushing to GitHub

## Notes
- User is new to coding — explain decisions clearly
- Do not over-engineer; keep the stack minimal
