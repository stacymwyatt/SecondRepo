# Node.js dependencies — installed locally via npm install
node_modules/

# SQLite database — contains local data, not for version control
contacts.db

# npm log files
npm-debug.log*

# Playwright generated output — these are local artifacts, not source code
playwright-report/
test-results/
